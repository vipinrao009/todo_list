import { createContext } from "react";

export default createContext(null) // createContext is the responsible for creating the parent component . if we persist any states then we can access the child component s from anywhere